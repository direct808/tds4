// @ts-nocheck
import * as grpc from './compiled'

import affiliateNetwork = grpc.tds.affiliate_network
import trafficSource = grpc.tds.traffic_source
import click = grpc.tds.click
import campaign = grpc.tds.campaign
import offer = grpc.tds.offer
import global = grpc.tds.global

export { affiliateNetwork, trafficSource, click, campaign, offer, global }
